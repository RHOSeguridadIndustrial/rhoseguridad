const box=document.getElementById('result'),token=new URLSearchParams(location.search).get('t')||'';
try{const response=await fetch(`https://mtpqpxcmngrsivntbgcc.supabase.co/functions/v1/loyalty-validate?t=${encodeURIComponent(token)}`);const data=await response.json();if(!response.ok||!data.valid)throw new Error(data.reason||'invalid');box.innerHTML=`<div class="validation-icon">✓</div><h1>Tarjeta vigente</h1><span class="tier-chip" style="background:${safeColor(data.color_hex)}">${escapeHtml(data.tier_name)}</span><h2>${escapeHtml(data.card_number)}</h2><p>${data.expires_at?`Vigente hasta ${new Date(data.expires_at).toLocaleDateString('es-MX')}`:'Vigencia sin límite'}</p><ul class="benefits" style="text-align:left">${(data.benefits||[]).map(x=>`<li>${escapeHtml(x)}</li>`).join('')}</ul><p class="privacy">La validación confirma el estado de la tarjeta sin exponer datos personales.</p>`;}catch{box.classList.add('invalid');box.innerHTML='<div class="validation-icon">×</div><h1>Tarjeta no válida</h1><p>El QR es incorrecto, venció o fue reemplazado. Solicita una nueva validación a RHO.</p>';}
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}function safeColor(value){return /^#[0-9a-f]{6}$/i.test(value||'')?value:'#01283b'}
